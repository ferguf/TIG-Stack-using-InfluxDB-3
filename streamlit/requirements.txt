# Core Streamlit
streamlit>=1.35.0
streamlit-extras

# Visualization (The Plotly Engine)
plotly>=5.18.0
streamlit-plotly-events>=0.0.6

# Backend Connectivity
requests>=2.31.0
httpx>=0.25.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Database Bridge (Optional if Streamlit talks directly to DB)
# influxdb-client>=1.36.0
# psycopg2-binary>=2.9.9

# SVG Fallback (If you still use the drawsvg engine for other parts)
drawsvg>=2.0.0